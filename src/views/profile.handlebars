<div class="mt-5 ms-5 p-2">
    <div>
        <h2>Profile</h2>
        <div>
            <div>
                <div>
                    <h3>{{ this.first_name }} {{ this.last_name }}</h3>
                    <p>{{ this.email }}</p>
                </div>
            </div>
            <div>
                tipo de cuenta: {{this.role}}
            </div>
        </div>
        <div>
            <button onclick="window.location.href = '/api/auth/logout'" type="button" class="btn btn-danger">Logout</button>
            <button onclick="window.location.href = '/api/products'" type="button" class="btn btn-success">Seguir Comprando</button>
        </div>
    </div>
</div>